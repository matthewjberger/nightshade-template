[package]
name = "example-plugin"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
nightshade = { version = "0.6.23", default-features = false, features = ["plugins"] }
