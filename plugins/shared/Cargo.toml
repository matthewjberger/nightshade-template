[package]
name = "plugins-shared"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { version = "0.6.28", default-features = false, features = ["plugins"] }
serde = { version = "1.0", features = ["derive"] }
postcard = "1.0"
